version: '3'
services:
   redis-server:
     image: "redis"
   node-app: 
      build:
      
        dockerfile: Dockerfile
        context: ./
      container_name: app_node
      ports: 
         - "80:3000"
